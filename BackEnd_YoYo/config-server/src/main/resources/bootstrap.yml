server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    vault:
      uri: http://vault:8200
      token: "myroot"
      authentication: token
      kv:
        backend: config-server-secret
        application-name: config-server
        profiles: dev